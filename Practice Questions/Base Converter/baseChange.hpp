#include <bits/stdc++.h>
using namespace std;

class baseChange{
    unsigned int n,k;
    string kbase;
    public:
        baseChange();
        void print();
        void convertNumber(int);
        void mapToChar(int);
};